database = %APPDATA%\Kodi\userdata\addon_data\service.watchedlist\watchedlist.db
movies = SELECT title FROM movie_watched WHERE playCount>0 ORDER BY title
episodes = SELECT title,season,episode FROM tvshows JOIN episode_watched USING(idShow) WHERE playCount>0 ORDER BY title,season,episode
font = Arial
fontsize = 16
width = 600
height = 400
